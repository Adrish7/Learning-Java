
public class toString {

	public static void main(String[] args) {
		// the toString method returns a string that textually represents the object like its address
		// you can use this to easily print out all the attributes of a class in one go
		// just create this 
		
		public String toString() {
		return attribute1+attribute2;}
				

	}

	// then later call this string when printing 
	System.out.println(class_name.toString());
	// you don't even need to call the to string you can just say Sysout(class_name);
	

}
