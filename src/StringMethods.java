import java.util.jar.Attributes.Name;

public class StringMethods {

	public static void main(String[] args) {
		name.equalsIgnoreCase(); // for checking if a string equals something without being case sensitive 
        char name = name.charAt(0); returns index 0 of the name 
        .indexof("b"); shows the index of this character 
        name.isEmpty
        name.toUpperCase();
        name.trim(); trims all the space 
        name.replace();
        }

}
